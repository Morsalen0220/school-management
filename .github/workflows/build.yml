name: Build
on: [push]
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and build
        run: |
          npm ci
          npm run build
          npm run generate:gui
          npm run generate:bundle

      - name: Push 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: bundle
          folder: ./bundle
